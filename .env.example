# Environment variables for Dawn Brew landing page
# This is a static site with no backend, so no environment variables are required
# for local development or production.

# All deployment is handled through Vercel dashboard settings
# No client-side or server-side environment variables are used